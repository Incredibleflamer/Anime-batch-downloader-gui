<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MarketPlace</title>
    <link rel="stylesheet" href="./css/marketplace.css" />
  </head>
  <body>
    <h1>MarketPlace</h1>
    <div class="providers-grid" id="providerGrid"></div>
    <div
      id="miniLoaderOverlay"
      style="
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.4);
        z-index: 9999;
        align-items: center;
        justify-content: center;
      "
    >
      <div
        style="
          padding: 1rem 2rem;
          background: #1e1e1e;
          border-radius: 10px;
          color: white;
          font-size: 1rem;
        "
      >
        Processing...
      </div>
    </div>
  </body>
  <script src="./js/libs/sweetalert.js"></script>
  <script src="./js/marketplace.js"></script>
  <script>
    let InstalledProvider = JSON.parse(
      `<%- JSON.stringify(providers).replace(/</g, '\\u003c').replace(/\\/g, '\\\\').replace(/"/g, '\\"') %>`
    );
    Init(`<%= AnimeManga %>`, `<%= AppVersion %>`);
  </script>
</html>
